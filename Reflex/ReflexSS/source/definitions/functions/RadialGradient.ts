
declare namespace Reflex.SS
{
	export interface Namespace
	{
		/** */
		radialGradient(value: CssValue, ...values: CssValue[]): Call;
		/** */
		radialGradient(values: CssValue[][]): Call;
		/** */
		"radial-gradient"(value: CssValue, ...values: CssValue[]): Call;
		/** */
		"radial-gradient"(values: CssValue[][]): Call;
	}
}
