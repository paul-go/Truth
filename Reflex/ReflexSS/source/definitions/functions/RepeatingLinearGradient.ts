
declare namespace Reflex.SS
{
	export interface Namespace
	{
		/** */
		repeatingLinearGradient(value: CssValue, ...values: CssValue[]): Call;
		/** */
		repeatingLinearGradient(values: CssValue[][]): Call;
		/** */
		"repeating-linear-gradient"(value: CssValue, ...values: CssValue[]): Call;
		/** */
		"repeating-linear-gradient"(values: CssValue[][]): Call;
	}
}
